kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  labels:
    app: publisher
  name: publisher-gopath
  namespace: publishing-bot
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
  storageClassName: ssd
